---
phase: 01-admin-shell-shared-action-infrastructure
plan: 05
type: execute
wave: 3
depends_on: ["01-01", "01-02", "01-03", "01-04"]
files_modified: []
autonomous: false
requirements: [SHELL-01, SHELL-02, SHELL-03, SHELL-06]

must_haves:
  truths:
    - "A human has visually confirmed the admin nav icon, role gate, and 4-group navigation work end-to-end on both a running WEB session and a running MOBILE (or MOBILE-in-browser) session"
  artifacts: []
  key_links: []
---

<objective>
Close the loop on Phase 1 with a single human-verify checkpoint proving the shell and role gate work live, on both platforms, with a real admin and a real non-admin account — something the automated test suites (01-01 through 01-04) cannot fully prove on their own (they test components in isolation, not the live running app). Per goal-backward reachability: every artifact this phase produces (`AdminPanel`, extended `BottomNavBar`, extended `Dashboard`, `useAdminAction`) has now been built and unit-tested by 01-01 through 01-04 — this plan is the final "does it actually work when you click it" gate before Phase 2 starts building real content on top of this shell.

This plan intentionally contains no implementation tasks (checkpoint-only, per the "checkpoint + implementation in same plan" split rule) — all code changes were completed in 01-01 through 01-04.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-admin-shell-shared-action-infrastructure/01-02-SUMMARY.md
@.planning/phases/01-admin-shell-shared-action-infrastructure/01-03-SUMMARY.md
</context>

<tasks>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 1: Human verification of the admin shell end-to-end on WEB and MOBILE</name>
  <files></files>
  <action>Run WEB and MOBILE dev servers and manually exercise the admin nav icon, role gate, and 4-group navigation as described below; no code changes in this task.</action>
  <what-built>
    A new "Admin" nav icon (mirrored on WEB and MOBILE) that only an admin user sees, opening a full-screen panel with working navigation between 4 placeholder groups (Usuários, Cartões e Autorizações de Compra, Faturamento, Solicitações). A shared `useAdminAction` hook exists and is fully unit-tested (confirm-before-execute, double-submit lock, success/error feedback, danger styling for heavy actions) — Phase 2 will be the first phase to wire it into a real mutating action.
  </what-built>
  <how-to-verify>
    WEB (`cd WEB && npm run dev`, open http://localhost:3000):
    1. Log in with the admin credentials on file (CPF 11111111111 / senha admin999, per project memory) — confirm a new "Admin" icon (shield) appears as the 6th item in the bottom nav bar, alongside Início/Cartões/Limite/Shop/Perfil.
    2. Tap it — confirm a full-screen panel opens with 4 tabs (Usuários, Cartões e Autorizações de Compra, Faturamento, Solicitações), "Usuários" active by default, and the standard header/bottom-nav are hidden while inside it.
    3. Tap each of the other 3 tabs — confirm the visible content switches to that group's placeholder ("Em construção — chega na próxima fase.") each time.
    4. Tap the back button — confirm you land back on the home view with the normal header and bottom-nav (including the Admin icon) restored.
    5. Log out, log in with a non-admin test user — confirm the Admin icon is absent from the bottom nav bar, and that the existing Profile → "Painel do Administrador" button (unrelated old entry point) still behaves as it did before this phase (unchanged, out of scope).

    MOBILE (`cd MOBILE && npm run dev:mobile`, open http://localhost:3002 or an emulator):
    6. Repeat steps 1-5 on MOBILE — confirm identical behavior (icon presence/absence, 4-tab navigation, back button, hidden header/nav while inside the panel).

    Automated coverage already run and green (informational, no need to re-run manually): `WEB && npx vitest run`, `MOBILE && npx vitest run`, `WEB && npx tsc --noEmit`, `MOBILE && npx tsc --noEmit` — all executed as part of 01-01 through 01-04's own verification.
  </how-to-verify>
  <resume-signal>Type "approved" once all 6 steps check out on both platforms, or describe exactly what broke (which step, which platform).</resume-signal>
  <verify>
    <human-check>Human walks through the 6 steps above on WEB and MOBILE and confirms each one.</human-check>
  </verify>
  <done>Human has typed "approved" after confirming the admin nav icon, role gate, 4-group navigation, and panel exit all work correctly on both WEB and MOBILE.</done>
</task>

</tasks>

<artifacts_produced>
None — this plan verifies artifacts produced by 01-01 through 01-04.
</artifacts_produced>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Human verification ↔ automated test coverage | This checkpoint exists specifically because component-level unit tests (01-01/01-02/01-03/01-04) cannot prove the live app wires everything together correctly end-to-end |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-07 | Repudiation | Role-gate regression slipping past automated tests | mitigate | Human explicitly re-verifies the non-admin case (step 5/6) live, against a real login, not a mocked `useAuth()` — catches any drift between the unit-test mock and real `AuthContext` behavior |
</threat_model>

<verification>
Human checkpoint approval is the verification for this plan.
</verification>

<success_criteria>
ROADMAP Phase 1 success criteria 1-3 confirmed working end-to-end, live, on both WEB and MOBILE, by a human — closing the phase.
</success_criteria>

<output>
Create `.planning/phases/01-admin-shell-shared-action-infrastructure/01-05-SUMMARY.md` when done
</output>
