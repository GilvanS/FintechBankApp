---
phase: 01-admin-shell-shared-action-infrastructure
plan: 03
type: execute
wave: 2
depends_on: ["01-02"]
files_modified:
  - MOBILE/src/components/AdminPanel.tsx
  - MOBILE/tests/AdminPanel.test.tsx
  - MOBILE/src/components/BottomNavBar.tsx
  - MOBILE/tests/BottomNavBar.test.tsx
  - MOBILE/src/components/Dashboard.tsx
autonomous: true
requirements: [SHELL-01, SHELL-02, SHELL-03, SHELL-06]

must_haves:
  truths:
    - "An admin user sees the same new 'Admin' bottom-nav icon on MOBILE as on WEB (SHELL-06 parity)"
    - "Tapping it opens the same full-screen 4-group admin panel on MOBILE"
    - "A non-admin user on MOBILE never sees the icon and cannot render the panel even if the view is forced open"
    - "Backing out returns to the normal MOBILE dashboard home view with header/bottom-nav restored"
  artifacts:
    - path: "MOBILE/src/components/AdminPanel.tsx"
      provides: "MOBILE mirror of WEB/components/AdminPanel.tsx — identical behavior, MOBILE import paths"
      exports: ["AdminPanel"]
    - path: "MOBILE/src/components/BottomNavBar.tsx"
      provides: "6th conditional nav item, gated on isAdmin, mirroring WEB"
      contains: "isAdmin"
    - path: "MOBILE/src/components/Dashboard.tsx"
      provides: "View union extended with 'adminPanel', same wiring as WEB/components/Dashboard.tsx"
      contains: "'adminPanel'"
  key_links:
    - from: "MOBILE/src/components/BottomNavBar.tsx"
      to: "MOBILE/src/components/Dashboard.tsx"
      via: "onNavigate('adminPanel') callback prop"
      pattern: "onNavigate\\('adminPanel'\\)"
    - from: "MOBILE/src/components/Dashboard.tsx"
      to: "MOBILE/src/components/AdminPanel.tsx"
      via: "case 'adminPanel' renders <AdminPanel onClose={...} />"
      pattern: "case 'adminPanel'"
---

<objective>
Port the WEB admin panel shell (01-02) to MOBILE file-for-file, per this codebase's deliberate duplicated-frontend-trees convention (`.planning/codebase/ARCHITECTURE.md`: "build in one platform, port file-for-file to the other" — never introduce a shared WEB/MOBILE package). Sequenced after 01-02 (wave 2, depends_on 01-02) rather than in parallel, so the tab labels, section IDs, role-gate pattern, and styling decisions are settled once on WEB and mechanically mirrored, not independently re-derived (avoids WEB/MOBILE drift on a brand-new pattern that has no prior precedent in this codebase).

Confirmed via direct inspection this session: `MOBILE/src/components/Dashboard.tsx` is line-for-line identical to `WEB/components/Dashboard.tsx` for every section this plan touches (imports, `type View`, `handleNavigate`, the `case 'admin':` render block, the three `topLevelView !== 'admin'` guards) — apply the exact same 5 edits from 01-02's Task 3, at the same relative locations. `MOBILE/src/components/BottomNavBar.tsx` differs from WEB's only cosmetically (adds `id={\`nav-${item.view}\`}` and `type="button"` per button) — the same `isAdmin`/6th-item change applies unchanged. `MOBILE/src/contexts/GlobalDialogContext.tsx` (used by 01-04, not this plan) is also byte-identical to WEB's — MOBILE does NOT use `IonAlert`/`IonActionSheet` for the existing app-wide confirm dialog despite Ionic being installed; this plan's AdminPanel therefore does not introduce any Ionic-specific dialog either, for consistency.

Purpose: WEB↔MOBILE parity (SHELL-06) for the admin entry point and grouped navigation.
Output: `AdminPanel.tsx` (new, MOBILE), extended `BottomNavBar.tsx` and `Dashboard.tsx` (MOBILE).
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-admin-shell-shared-action-infrastructure/01-02-SUMMARY.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Port AdminPanel shell to MOBILE</name>
  <files>MOBILE/src/components/AdminPanel.tsx, MOBILE/tests/AdminPanel.test.tsx</files>
  <read_first>WEB/components/AdminPanel.tsx (as built by 01-02 — the canonical source to port from, read it in full before writing the MOBILE copy); MOBILE/src/components/Admin.tsx lines 1-90 (confirm MOBILE's `Admin` component has the same `React.FC&lt;{ onClose: () => void }&gt;` signature and `useAuth()`/`useAppState()` usage as WEB's, so the port needs only import-path changes, not logic changes); MOBILE/src/context/AuthContext.tsx and MOBILE/src/types.ts line 151 (`role: 'user' | 'admin'` — confirm identical shape to WEB); MOBILE/tests/ui-components.test.tsx (existing MOBILE render-test style/mocking conventions to match, since MOBILE has no direct `Profile.test.tsx` equivalent).</read_first>
  <behavior>
    Identical to 01-02 Task 1's 4 behaviors (non-admin closes/renders nothing, admin sees 4 tabs with Usuários active by default, tab switching updates content, back button calls onClose) — same assertions, MOBILE import paths and MOBILE `useAuth`/`useAppState` mocks.
  </behavior>
  <action>
    Create `MOBILE/src/components/AdminPanel.tsx` as a file-for-file port of `WEB/components/AdminPanel.tsx`: identical `AdminSection` type, identical 4-item config array with the same Portuguese labels, identical role-gate `useEffect`, identical tab-switching state and placeholder body copy ("Em construção — chega na próxima fase."). Only change import paths to MOBILE's relative structure (`../context/AuthContext`, `../contexts/AppStateContext`, `lucide-react`, `motion/react` — all already dependencies of MOBILE per `MOBILE/package.json`). Do not add any `@ionic/react` import (`IonPage`, `IonAlert`, etc.) — this component follows the plain-React pattern `Admin.tsx`/`Dashboard.tsx` already use on MOBILE, not the Ionic page-wrapper pattern used by `MOBILE/src/pages/`.
  </action>
  <verify>
    <automated>cd MOBILE && npx vitest run tests/AdminPanel.test.tsx</automated>
  </verify>
  <acceptance_criteria>
    - `MOBILE/src/components/AdminPanel.tsx` exports `AdminPanel` (default) with behavior identical to the WEB version.
    - All 4 ported behaviors pass in `MOBILE/tests/AdminPanel.test.tsx`.
    - `grep -c "from '../services/api'" MOBILE/src/components/AdminPanel.tsx` returns `0` (zero API calls, same as WEB).
  </acceptance_criteria>
  <done>MOBILE's AdminPanel behaves identically to WEB's — same tabs, same role gate, same placeholder copy.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Port the 6th "Admin" nav item to MOBILE BottomNavBar</name>
  <files>MOBILE/src/components/BottomNavBar.tsx, MOBILE/tests/BottomNavBar.test.tsx</files>
  <read_first>WEB/components/BottomNavBar.tsx as extended by 01-02 Task 2 (the canonical `isAdmin` implementation to port); MOBILE/src/components/BottomNavBar.tsx (current MOBILE version — near-identical to WEB's pre-change version but each `&lt;button&gt;` additionally carries `id={\`nav-${item.view}\`}` and `type="button"` — preserve both on the new admin item too, consistent with the other 5 items in this file).</read_first>
  <behavior>Identical to 01-02 Task 2's 3 behaviors (5 items with isAdmin omitted/false, 6th "Admin" item when isAdmin true, clicking it calls onNavigate('adminPanel')).</behavior>
  <action>
    Apply the same change as 01-02 Task 2 to `MOBILE/src/components/BottomNavBar.tsx`: add `isAdmin?: boolean` to `BottomNavBarProps`, widen `onNavigate`'s union to include `'adminPanel'`, import `Shield` from `lucide-react`, and conditionally append `{ label: 'Admin', view: 'adminPanel', icon: Shield }` to the items array rendered — keeping the existing `id={\`nav-${item.view}\`}` and `type="button"` attributes on the generated button for the new item exactly as they already exist for the other 5.
  </action>
  <verify>
    <automated>cd MOBILE && npx vitest run tests/BottomNavBar.test.tsx</automated>
  </verify>
  <acceptance_criteria>
    - `MOBILE/src/components/BottomNavBar.tsx` behavior matches WEB's ported version exactly (same 3 test behaviors pass).
    - The new admin button carries `id="nav-adminPanel"` and `type="button"`, consistent with MOBILE's existing 5 items.
  </acceptance_criteria>
  <done>MOBILE BottomNavBar shows the same conditional 6th item as WEB.</done>
</task>

<task type="auto">
  <name>Task 3: Wire AdminPanel into MOBILE Dashboard's view state machine</name>
  <files>MOBILE/src/components/Dashboard.tsx</files>
  <read_first>WEB/components/Dashboard.tsx as edited by 01-02 Task 3 (the canonical diff to replicate); MOBILE/src/components/Dashboard.tsx lines 1-45, 81, 218-259, 700-724, 741, 770, 788-790 (confirmed this session to be line-for-line identical to WEB's pre-edit version at every one of these locations).</read_first>
  <action>
    Apply the identical 5 edits from 01-02 Task 3 to `MOBILE/src/components/Dashboard.tsx`, at the same relative line locations (import `AdminPanel from './AdminPanel'` beside `import Admin from './Admin'`; extend `type View` with `'adminPanel'`; add the `handleNavigate` branch for `'adminPanel'`; add `case 'adminPanel':` after `case 'admin':` with the identical wrapper div; extend both `topLevelView !== 'admin'` guards to also check `currentView !== 'adminPanel'`; add `isAdmin={user?.role === 'admin'}` to the existing `&lt;BottomNavBar /&gt;` call). Do not touch `MOBILE/src/App.tsx`'s own separate top-level `case 'admin':` branch (an unrelated, pre-existing code path that renders `Admin` directly outside of `Dashboard` — out of scope for this phase, not reachable via the new nav item).
  </action>
  <verify>
    <automated>cd MOBILE && npx tsc --noEmit</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "'adminPanel'" MOBILE/src/components/Dashboard.tsx` returns at least `4`, matching the WEB file's edit count.
    - `cd MOBILE && npx tsc --noEmit` passes with zero errors.
    - `cd MOBILE && npx vitest run` (full existing suite) still passes.
  </acceptance_criteria>
  <done>MOBILE's admin nav icon opens the same full-screen AdminPanel, hiding header/bottom-nav identically to WEB, and returns cleanly to home on close.</done>
</task>

</tasks>

<artifacts_produced>
- `MOBILE/src/components/AdminPanel.tsx` — exports `AdminPanel` (default), file-for-file port of WEB's
- `MOBILE/src/components/BottomNavBar.tsx` — extended: `isAdmin?: boolean` prop, `'adminPanel'` added to `onNavigate` union
- `MOBILE/src/components/Dashboard.tsx` — extended identically to WEB's Dashboard.tsx
- `MOBILE/tests/AdminPanel.test.tsx`, `MOBILE/tests/BottomNavBar.test.tsx` (new test files)
</artifacts_produced>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| BottomNavBar visibility ↔ AdminPanel render (MOBILE) | Same client-side-only role gate as WEB; MOBILE's Capacitor/Android WebView does not change this boundary |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-05 | Elevation of Privilege | MOBILE `handleNavigate('adminPanel')` reachability | mitigate | Identical mitigation to T-01-03 (01-02): nav item gated on `isAdmin`, `AdminPanel`'s own role-check `useEffect` is the defense-in-depth backstop, ported unchanged from WEB |
</threat_model>

<verification>
- `cd MOBILE && npx vitest run tests/AdminPanel.test.tsx tests/BottomNavBar.test.tsx` — both new suites green
- `cd MOBILE && npx tsc --noEmit` — no type errors
- `cd MOBILE && npx vitest run` — full existing suite still passes
</verification>

<success_criteria>
MOBILE delivers ROADMAP Phase 1 success criteria 1-3, matching WEB (01-02) — SHELL-06 parity achieved.
</success_criteria>

<output>
Create `.planning/phases/01-admin-shell-shared-action-infrastructure/01-03-SUMMARY.md` when done
</output>
