---
phase: 01-admin-shell-shared-action-infrastructure
plan: 04
type: tdd
wave: 2
depends_on: ["01-01"]
files_modified:
  - MOBILE/src/contexts/GlobalDialogContext.tsx
  - MOBILE/tests/GlobalDialogContext.test.tsx
  - MOBILE/src/hooks/useAdminAction.ts
  - MOBILE/tests/useAdminAction.test.ts
autonomous: true
requirements: [SHELL-04, SHELL-05]

must_haves:
  truths:
    - "MOBILE has the same confirm-gate, double-submit-locked, success/error-feedback admin action hook as WEB (SHELL-06 parity for shared infrastructure)"
    - "MOBILE's danger/heavy-confirm dialog variant renders identically to WEB's"
  artifacts:
    - path: "MOBILE/src/hooks/useAdminAction.ts"
      provides: "MOBILE mirror of WEB/hooks/useAdminAction.ts"
      exports: ["useAdminAction", "AdminActionOptions", "ActionTier"]
    - path: "MOBILE/src/contexts/GlobalDialogContext.tsx"
      provides: "DialogOptions extended with danger flag, mirroring WEB"
      contains: "danger?: boolean"
  key_links:
    - from: "MOBILE/src/hooks/useAdminAction.ts"
      to: "MOBILE/src/contexts/GlobalDialogContext.tsx"
      via: "useDialog().showDialog(...)"
      pattern: "showDialog\\("
---

<objective>
Port the WEB shared admin action hook (01-01) to MOBILE file-for-file. Sequenced after 01-01 (wave 2, depends_on 01-01) for the same reason 01-03 depends on 01-02: a brand-new pattern with no prior codebase precedent should be validated once on WEB, then mechanically mirrored — not independently re-derived and risk WEB/MOBILE behavioral drift on double-submit/confirmation logic specifically (the highest-leverage shared infrastructure this phase produces).

Confirmed via direct inspection this session: `MOBILE/src/contexts/GlobalDialogContext.tsx` is byte-identical to `WEB/contexts/GlobalDialogContext.tsx` (same `DialogOptions`, same `DialogProvider`, same non-theme-aware dark styling, same confirm/cancel button markup) — apply the identical `danger` extension. MOBILE has no `WEB/hooks/useCart.ts`-equivalent hooks directory yet (`MOBILE/src/hooks/` does not currently exist) — this plan creates it, matching WEB's `hooks/` convention.

Purpose: Every MOBILE admin mutating action in Phase 2-6 gets the identical double-submit/confirmation guarantee as WEB, from one shared, already-tested design.
Output: `MOBILE/src/hooks/useAdminAction.ts` (new), `MOBILE/src/contexts/GlobalDialogContext.tsx` (extended).
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-admin-shell-shared-action-infrastructure/01-01-SUMMARY.md
</context>

<tasks>

<task type="tdd" tdd="true">
  <name>Task 1: Port the danger/heavy-confirm variant to MOBILE's GlobalDialogContext</name>
  <files>MOBILE/src/contexts/GlobalDialogContext.tsx, MOBILE/tests/GlobalDialogContext.test.tsx</files>
  <read_first>WEB/contexts/GlobalDialogContext.tsx as extended by 01-01 Task 1 (the canonical diff — `danger?: boolean` on `DialogOptions`, `data-testid="dialog-confirm-button"` and `data-danger` on the confirm button, conditional `bg-red-600 hover:bg-red-500 text-white` vs `bg-volt-primary text-black hover:bg-[#b0f52b]` classNames); MOBILE/src/contexts/GlobalDialogContext.tsx (current MOBILE file — confirmed byte-identical to WEB's pre-change version).</read_first>
  <behavior>Identical to 01-01 Task 1's 3 behaviors (danger renders red + data-danger="true", default renders bg-volt-primary + data-danger="false", Cancel still works regardless of danger).</behavior>
  <action>
    Apply the identical change from 01-01 Task 1 to `MOBILE/src/contexts/GlobalDialogContext.tsx` — same `DialogOptions.danger?: boolean` field, same `data-testid`/`data-danger` attributes and conditional className swap on the confirm button, no other logic changes. Write `MOBILE/tests/GlobalDialogContext.test.tsx` using the same `@testing-library/react` render/fireEvent approach as the WEB test, adapted to MOBILE's existing test conventions in `MOBILE/tests/ui-components.test.tsx`.
  </action>
  <verify>
    <automated>cd MOBILE && npx vitest run tests/GlobalDialogContext.test.tsx</automated>
  </verify>
  <acceptance_criteria>
    - `MOBILE/src/contexts/GlobalDialogContext.tsx`'s `DialogOptions` includes `danger?: boolean`, behavior identical to WEB's ported version.
    - All 3 ported behaviors pass.
    - `cd MOBILE && npx vitest run` (full existing suite) still passes — no regression.
  </acceptance_criteria>
  <done>MOBILE's dialog danger variant is byte-behavior-identical to WEB's.</done>
</task>

<task type="tdd" tdd="true">
  <name>Task 2: Port useAdminAction to MOBILE</name>
  <files>MOBILE/src/hooks/useAdminAction.ts, MOBILE/tests/useAdminAction.test.ts</files>
  <read_first>WEB/hooks/useAdminAction.ts as built by 01-01 Task 2 (the canonical source — full implementation, `ActionTier`, `AdminActionOptions&lt;T&gt;`, the `useRef`-backed double-submit guard, success/error copy resolution); MOBILE/src/contexts/GlobalDialogContext.tsx as extended by this plan's Task 1 (`useDialog()` shape, now including `danger`).</read_first>
  <behavior>Identical to 01-01 Task 2's 5 behaviors (no execution before confirm, double-submit block while busy, success dialog + lock release on resolve, error dialog + lock release on reject, danger styling only for tier 'heavy').</behavior>
  <action>
    Create `MOBILE/src/hooks/useAdminAction.ts` as a file-for-file port of `WEB/hooks/useAdminAction.ts` — identical `ActionTier`, `AdminActionOptions&lt;T&gt;`, `useAdminAction()` returning `{ run, isBusy }`, identical `useRef`-based synchronous re-entry guard, identical Portuguese default success/error copy. Only change the `useDialog` import path to `../contexts/GlobalDialogContext` (MOBILE's relative structure). Mock `useDialog` via `vi.mock` in the test exactly as done in `WEB/tests/useAdminAction.test.ts`, adapted to MOBILE's Vitest config (`MOBILE/vite.config.ts` test settings, `MOBILE/tests/setup.ts`).
  </action>
  <verify>
    <automated>cd MOBILE && npx vitest run tests/useAdminAction.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `MOBILE/src/hooks/useAdminAction.ts` exports `useAdminAction`, `AdminActionOptions`, `ActionTier`, behavior identical to WEB's.
    - All 5 ported behaviors pass.
    - Double-submit test asserts the wrapped action mock is called exactly once across two rapid `run()` calls (same assertion as WEB).
    - `cd MOBILE && npx tsc --noEmit` passes.
  </acceptance_criteria>
  <done>MOBILE has the identical confirm-gated, double-submit-protected, success/error-feedback hook as WEB, ready for Phase 2+ to consume on both platforms.</done>
</task>

</tasks>

<artifacts_produced>
- `MOBILE/src/hooks/useAdminAction.ts` — exports `useAdminAction()`, `AdminActionOptions<T>`, `ActionTier` (new `MOBILE/src/hooks/` directory)
- `MOBILE/src/contexts/GlobalDialogContext.tsx` — `DialogOptions.danger?: boolean` (extended, not replaced)
- `MOBILE/tests/GlobalDialogContext.test.tsx`, `MOBILE/tests/useAdminAction.test.ts` (new test files)
</artifacts_produced>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| MOBILE Admin UI → shared action hook | Same boundary as WEB (T-01-01/T-01-02 in 01-01), mirrored for MOBILE |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-06 | Tampering | MOBILE future admin mutating actions (double-submit) | mitigate | Identical mitigation to T-01-01: `useRef`-backed busy lock, ported unchanged from WEB |
</threat_model>

<verification>
- `cd MOBILE && npx vitest run tests/GlobalDialogContext.test.tsx tests/useAdminAction.test.ts` — both new suites green
- `cd MOBILE && npx vitest run` — full existing suite still passes
- `cd MOBILE && npx tsc --noEmit` — no new type errors
</verification>

<success_criteria>
MOBILE's shared confirmation/mutation infrastructure matches WEB's exactly (ROADMAP Phase 1 success criteria 4-5, infrastructure-level parity — SHELL-06).
</success_criteria>

<output>
Create `.planning/phases/01-admin-shell-shared-action-infrastructure/01-04-SUMMARY.md` when done
</output>
