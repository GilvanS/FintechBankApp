---
phase: 01-admin-shell-shared-action-infrastructure
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - WEB/components/AdminPanel.tsx
  - WEB/tests/AdminPanel.test.tsx
  - WEB/components/BottomNavBar.tsx
  - WEB/tests/BottomNavBar.test.tsx
  - WEB/components/Dashboard.tsx
autonomous: true
requirements: [SHELL-01, SHELL-02, SHELL-03, SHELL-06]

must_haves:
  truths:
    - "An admin user sees a new, dedicated 'Admin' icon in the bottom nav bar (not reached through Profile) on WEB"
    - "Tapping it opens a full-screen admin panel showing 4 group tabs: Usuários, Cartões e Autorizações de Compra, Faturamento, Solicitações — mirroring the 4 Swagger admin tag groups"
    - "Switching tabs updates the visible section without leaving the panel"
    - "A non-admin user never sees the nav icon, and if the admin panel is reached anyway (e.g. by directly manipulating client state), it refuses to render and returns the user home — defense in depth, same double-check pattern already used for the existing 'admin' view in Dashboard.tsx"
    - "Backing out of the admin panel returns to the normal dashboard home view with the standard header and bottom nav restored"
  artifacts:
    - path: "WEB/components/AdminPanel.tsx"
      provides: "Role-gated admin panel shell with internal 4-group tab state"
      exports: ["AdminPanel"]
    - path: "WEB/components/BottomNavBar.tsx"
      provides: "6th conditional nav item, gated on an isAdmin prop"
      contains: "isAdmin"
    - path: "WEB/components/Dashboard.tsx"
      provides: "View union extended with 'adminPanel', handleNavigate branch, switch case, render-guard updates"
      contains: "'adminPanel'"
  key_links:
    - from: "WEB/components/BottomNavBar.tsx"
      to: "WEB/components/Dashboard.tsx"
      via: "onNavigate('adminPanel') callback prop, called from the new nav item's onClick"
      pattern: "onNavigate\\('adminPanel'\\)"
    - from: "WEB/components/Dashboard.tsx"
      to: "WEB/components/AdminPanel.tsx"
      via: "case 'adminPanel' renders <AdminPanel onClose={...} />, same wrapper pattern as the existing case 'admin'"
      pattern: "case 'adminPanel'"
---

<objective>
Deliver the vertical slice of Phase 1: a real, navigable, role-gated admin panel entry point on WEB with working navigation between 4 placeholder functional groups. Per REQUIREMENTS.md SHELL-01/02/03/06 (no CONTEXT.md exists for this phase — discuss-phase was skipped; scope taken directly from ROADMAP.md Phase 1 + REQUIREMENTS.md).

Deviation from milestone RESEARCH.md/ARCHITECTURE.md: ARCHITECTURE.md recommends pre-creating a `components/admin/` subfolder with 4 separate section files (`AdminUsers.tsx`, `AdminCardsAuth.tsx`, `AdminBilling.tsx`, `AdminRequests.tsx`) now. This plan keeps the 4 group sections as inline placeholder blocks inside `AdminPanel.tsx` instead — they currently have zero functional content (Phase 1 is shell-only), and creating 4 near-empty files now would just be substantially rewritten by Phases 2-6 when real content arrives (violates CLAUDE.md's "never create files unless absolutely necessary"). Extraction into `components/admin/<Name>.tsx` happens starting Phase 2 (Usuários), once a section has real logic to hold. This is a planner discretion call, not a locked decision — SHELL-03's "navigation across the four functional groups" is still fully delivered by this plan, just as inline tab content rather than 4 separate files.

Purpose: Establishes the one true entry point every later admin phase (2-6) plugs its real content into, replacing the flat 833-line `Admin.tsx` shape with grouped navigation, without touching `Admin.tsx`'s existing internals (its retirement is decided in Phase 6, per STATE.md's recorded blocker).
Output: `AdminPanel.tsx` (new), extended `BottomNavBar.tsx` and `Dashboard.tsx`.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Create the AdminPanel shell — role gate + 4-group tab navigation</name>
  <files>WEB/components/AdminPanel.tsx, WEB/tests/AdminPanel.test.tsx</files>
  <read_first>WEB/components/Admin.tsx lines 1-90 and 255-339 (imports, `StatCard`, `Admin: React.FC&lt;{ onClose: () => void }&gt;` signature, `useAuth()`/`useAppState()` usage, the exact header markup/classNames to visually match: `closeBtnClass`, `titleClass`, `subTextClass`, `sectionCardClass`, back-arrow button with `ArrowLeft`+`Shield` icons, `data-testid="admin-page"`/`admin-header`/`admin-title` pattern); WEB/context/AuthContext.tsx (`useAuth()` returns `{ user, ... }`, `user.role: 'user' | 'admin'` from WEB/types.ts line 140); WEB/contexts/AppStateContext.tsx (confirm `useAppState()` exposes `theme: 'midnight' | 'yellow'`, matching `Admin.tsx`'s `isMidnight` derivation); WEB/tests/Profile.test.tsx (existing render-test structure/mocking style to mirror for AdminPanel.test.tsx).</read_first>
  <behavior>
    - Test 1: rendering `&lt;AdminPanel onClose={fn} /&gt;` with a mocked `useAuth()` returning `user.role === 'user'` calls `onClose` (via an effect) and renders nothing.
    - Test 2: rendering with `user.role === 'admin'` shows 4 tab buttons labeled "Usuários", "Cartões e Autorizações de Compra", "Faturamento", "Solicitações", with "Usuários" active by default.
    - Test 3: clicking the "Faturamento" tab switches the visible placeholder content to the Faturamento section and updates which tab is marked active.
    - Test 4: clicking the back/close button calls `onClose`.
  </behavior>
  <action>
    Create `AdminPanel: React.FC&lt;{ onClose: () => void }&gt;`. Define `type AdminSection = 'users' | 'cardsAuth' | 'billing' | 'requests'` and a config array of `{ id: AdminSection, label: string }` pairs with the 4 Portuguese labels above (matching the 4 Swagger admin tag groups: "Admin - Usuários", "Admin - Cartões e Autorizações de Compra", "Admin - Faturamento", "Admin - Solicitações"). Use `const [section, setSection] = useState&lt;AdminSection&gt;('users')`. Add a `useEffect` that calls `onClose()` whenever `user?.role !== 'admin'` (mirrors the existing double-check pattern in `Dashboard.tsx`'s admin-gate `useEffect`), and return `null` synchronously when the role check fails so nothing renders even for the one frame before the effect fires. Reuse `Admin.tsx`'s header visual pattern (back button + `Shield` icon + title "Painel Administrativo" + `sectionCardClass`/`titleClass`-equivalent Tailwind classes derived the same way from `isMidnight`) for a consistent look, and a horizontal tab strip below it rendering the 4 section buttons (active tab visually distinguished, same `primaryOutlineBtnClass`/`neutralBtnClass` two-state pattern already used in `Admin.tsx`). Each section body is a simple placeholder: a centered card stating the group's Portuguese label and "Em construção — chega na próxima fase." Do not import or call any `admin*` API function — this component makes zero network calls in Phase 1.
  </action>
  <verify>
    <automated>cd WEB && npx vitest run tests/AdminPanel.test.tsx</automated>
  </verify>
  <acceptance_criteria>
    - `AdminPanel` is exported as default from `WEB/components/AdminPanel.tsx`.
    - All 4 behaviors in `WEB/tests/AdminPanel.test.tsx` pass.
    - Component makes no `services/api.ts` imports (grep confirms: `grep -c "from '../services/api'" WEB/components/AdminPanel.tsx` returns `0`).
  </acceptance_criteria>
  <done>AdminPanel renders only for admin users, shows working navigation across the 4 required functional groups, and closes cleanly via its own back button and via the role-gate effect.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Add a conditional 6th "Admin" nav item to BottomNavBar</name>
  <files>WEB/components/BottomNavBar.tsx, WEB/tests/BottomNavBar.test.tsx</files>
  <read_first>WEB/components/BottomNavBar.tsx (full file, 85 lines — note the module-level `navItems` const array, `BottomNavBarProps.onNavigate` union type currently `'home' | 'cards' | 'shop' | 'profile' | 'limit'`, and the `.map(item => ...)` render loop keyed by `item.view`).</read_first>
  <behavior>
    - Test 1: rendering `&lt;BottomNavBar currentView="home" onNavigate={fn} /&gt;` (no `isAdmin` prop) renders exactly 5 nav buttons — no admin item present.
    - Test 2: rendering with `isAdmin={true}` renders a 6th button labeled "Admin".
    - Test 3: clicking the "Admin" button calls `onNavigate('adminPanel')`.
  </behavior>
  <action>
    Add `isAdmin?: boolean` to `BottomNavBarProps` (default `false` via destructuring default). Widen `onNavigate`'s parameter union to include `'adminPanel'`. Import `Shield` from `lucide-react` (already imported by `Admin.tsx` elsewhere in this codebase — same icon, zero new dependency) alongside the existing `Home, CreditCard, ShoppingBag, User, Sliders` import. Keep the existing module-level `navItems` array (the 5 base items) untouched; inside the component body compute `const items = isAdmin ? [...navItems, { label: 'Admin', view: 'adminPanel', icon: Shield }] : navItems;` and map over `items` instead of `navItems` in the render. Do not change any existing item's label, icon, or order.
  </action>
  <verify>
    <automated>cd WEB && npx vitest run tests/BottomNavBar.test.tsx</automated>
  </verify>
  <acceptance_criteria>
    - `BottomNavBarProps` includes `isAdmin?: boolean` and `onNavigate`'s union includes `'adminPanel'`.
    - All 3 behaviors in `WEB/tests/BottomNavBar.test.tsx` pass.
    - Default (`isAdmin` omitted) render output is unchanged from before this task (still exactly 5 items).
  </acceptance_criteria>
  <done>BottomNavBar shows a 6th "Admin" item only when explicitly told the current user is an admin, and that item navigates to 'adminPanel'.</done>
</task>

<task type="auto">
  <name>Task 3: Wire AdminPanel into Dashboard's view state machine</name>
  <files>WEB/components/Dashboard.tsx</files>
  <read_first>WEB/components/Dashboard.tsx lines 1-45 (imports, note `import Admin from './Admin';` at line 22 to add `import AdminPanel from './AdminPanel';` beside it), lines 81 (the `type View = ...` union — 'admin' already present, add `'adminPanel'`), lines 218-259 (`handleNavigate` — the `if (newView === 'admin') { setPreviousView(currentView); setCurrentView('admin'); return; }` block at lines 234-238 to copy the pattern from), lines 700-724 (`case 'admin':` render block wrapping `&lt;Admin onClose={...} /&gt;` in `&lt;div className="min-h-full pb-20 ${theme==='midnight' ? 'bg-[#0f0f0f]' : 'bg-volt-yellow'}"&gt;`), lines 741, 770, 788-790 (the three `topLevelView !== 'admin'` guards around `&lt;Header /&gt;`, the hidden-header-menu button, and `&lt;BottomNavBar /&gt;` — the last one already renders `&lt;BottomNavBar currentView={currentView} onNavigate={(view) => handleNavigate(view)} theme={theme} /&gt;` with no `isAdmin` prop).</read_first>
  <action>
    Five precise edits to `WEB/components/Dashboard.tsx`: (1) add `import AdminPanel from './AdminPanel';` next to the existing `import Admin from './Admin';`. (2) extend the `type View = ...` union to include `'adminPanel'`. (3) in `handleNavigate`, add a new `if (newView === 'adminPanel') { setPreviousView(currentView); setCurrentView('adminPanel'); return; }` branch (place it beside the existing `'admin'` branch, before the generic fallback). (4) add a new `case 'adminPanel':` to the render switch, immediately after `case 'admin':`, returning the same wrapper `&lt;div className={\`min-h-full pb-20 ${theme === 'midnight' ? 'bg-[#0f0f0f]' : 'bg-volt-yellow'}\`}&gt;&lt;AdminPanel onClose={() => handleNavigate('home')} /&gt;&lt;/div&gt;` (note: unlike the `'admin'` case's `onClose`, do not call `navigateTo('dashboard')` here — 'adminPanel' is purely Dashboard-local state, it never touches the AuthContext-level `topLevelView`). (5) update the two `topLevelView !== 'admin' && ...` guards around `&lt;Header /&gt;` (line 741) and the hidden-header-menu button (line 770) to also read `topLevelView !== 'admin' && currentView !== 'adminPanel' && ...` — this hides the standard header while inside the new admin panel, matching how the existing 'admin' view hides it. Leave the `&lt;BottomNavBar /&gt;` allow-list condition's view array untouched ('adminPanel' is correctly absent from it, so the bar auto-hides while inside the panel) but add the `isAdmin={user?.role === 'admin'}` prop to the existing `&lt;BottomNavBar currentView={currentView} onNavigate={(view) => handleNavigate(view)} theme={theme} /&gt;` call so the new nav item actually appears on the other 5 views.
  </action>
  <verify>
    <automated>cd WEB && npx tsc --noEmit</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "'adminPanel'" WEB/components/Dashboard.tsx` returns at least `4` (View union, handleNavigate branch, switch case, isAdmin-gated BottomNavBar usage does not itself contain the string but the two header guards plus case plus handleNavigate branch do).
    - `cd WEB && npx tsc --noEmit` passes with zero errors.
    - `cd WEB && npx vitest run` (full existing suite) still passes — no regression to any view Dashboard already handles.
  </acceptance_criteria>
  <done>An admin user tapping the new nav icon lands on a full-screen AdminPanel with header/bottom-nav hidden, and backing out returns cleanly to the home view with header/bottom-nav restored.</done>
</task>

</tasks>

<artifacts_produced>
- `WEB/components/AdminPanel.tsx` — exports `AdminPanel` (default), role-gated, 4-tab shell
- `WEB/components/BottomNavBar.tsx` — extended: `isAdmin?: boolean` prop, `'adminPanel'` added to `onNavigate` union
- `WEB/components/Dashboard.tsx` — extended: `View` union `+ 'adminPanel'`, `handleNavigate` branch, `case 'adminPanel'`, updated Header-hide guards, `isAdmin` prop passed to `BottomNavBar`
- `WEB/tests/AdminPanel.test.tsx`, `WEB/tests/BottomNavBar.test.tsx` (new test files)
</artifacts_produced>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| BottomNavBar visibility ↔ AdminPanel render | Client-side-only role gate; the real admin API boundary (`authenticateAdmin` middleware) is untouched by this phase since AdminPanel makes zero API calls |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-03 | Elevation of Privilege | `handleNavigate('adminPanel')` reachability | mitigate | Nav item only rendered when `isAdmin` prop is true (server-derived `user.role`), AND `AdminPanel`'s own `useEffect` independently re-checks `user.role === 'admin'` and calls `onClose()` otherwise — a non-admin reaching the view via client-state manipulation (e.g. devtools) still gets bounced, same defense-in-depth pattern as the existing `'admin'` view |
| T-01-04 | Information Disclosure | Empty admin panel shell visible to a client-tampered non-admin session | accept | Phase 1's AdminPanel contains zero user data and makes zero admin API calls — worst case a tampered non-admin session briefly sees 4 empty group labels before the role-gate effect closes it; real data/actions (and their server-side enforcement) arrive starting Phase 2 |
</threat_model>

<verification>
- `cd WEB && npx vitest run tests/AdminPanel.test.tsx tests/BottomNavBar.test.tsx` — both new suites green
- `cd WEB && npx tsc --noEmit` — no type errors
- `cd WEB && npx vitest run` — full existing suite still passes
</verification>

<success_criteria>
WEB delivers ROADMAP Phase 1 success criteria 1-3: a dedicated nav entry point outside Profile, role-gated access, and grouped navigation across the 4 functional groups.
</success_criteria>

<output>
Create `.planning/phases/01-admin-shell-shared-action-infrastructure/01-02-SUMMARY.md` when done
</output>
